{
    "TitleInput": {
        "type": "text",
        "label": "Title",
        "required": true
    },
    
    "InstructorInput": {
        "type": "text",
        "label": "Instructor Name",
        "required": true
    },
    
    "StartInput": {
        "type": "container",
        
        "contents": {

            "DateInput": {
                "label": "Start Date",
                "width": "75%",
                "type": "date",
                "required":true
            },

            "TimeInput": {
                "width": "25%",
                "type": "time",
                "required":true
            }
        }
    },

    "EndInput": {
        "type": "container",
        
        "contents": {

            "DateInput": {
                "label": "End Date",
                "width": "75%",
                "type": "date",
                "required":true
            },

            "TimeInput": {
                "width": "25%",
                "type": "time",
                "required":true
            }
        }
    },

    "SubmitButton": {
        "type": "button",
        "text": "Add",
        "submit": true
    }
}