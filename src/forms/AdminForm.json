{
    "TitleInput": {
        "type": "text",
        "label": "Title",
        "required": true
    },
    
    "InstructorInput": {
        "type": "text",
        "label": "Instructor Name",
        "required": true
    },
    
    "StartInput": {
        "type": "container",
        
        "contents": {

            "DateInput": {
                "label": "Start Date",
                "width": "75%",
                "type": "date",
                "required":true
            },

            "TimeInput": {
                "width": "25%",
                "type": "time",
                "required":true
            }
        }
    },

    "EndInput": {
        "type": "container",
        
        "contents": {

            "DateInput": {
                "label": "End Date",
                "width": "75%",
                "type": "date",
                "required":true
            },

            "TimeInput": {
                "width": "25%",
                "type": "time",
                "required":true
            }
        }
    },

    "LocationInput": {
        "type": "text",
        "label": "Loctaion",
        "required": true
    },

    "MaxAtendeesInput": {
        "type": "number",
        "label": "Maximum atendees",
        "required": true
    },

    "FormButtons": {
        "type": "container",
        "center": true,
        
        "contents": {
            "SubmitButton": {
                "type": "button",
                "text": "Add",
                "submit": true
            },

            "ResetButton": {
                "type": "button",
                "text": "Reset",
                "reset": true
            }
        }
    }
}